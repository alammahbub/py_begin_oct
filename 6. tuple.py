# in python tuple is data that are enclosed by parenthesis
# useally we store data in tuple, if it is fixed over the whole program
coordinates = (4,5)
#coordinates[1] = 3 will produce error cause, tuple is immutable
# we can access data using index
print(coordinates[0])
